const cosmosEndpoint = require('./cosmos/cosmosEndpoint');
const cosmosUpdate = require('./cosmos/cosmosUpdate');
const cosmosInstaller = require('./cosmos/cosmosInstaller');
const nodeInfo = require('./cosmos/nodeInfo');

module.exports = {
    cosmosEndpoint,
    cosmosUpdate,
    cosmosInstaller,
    nodeInfo
};